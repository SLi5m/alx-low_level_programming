#include "main.h"

/**
 * _islower - checks for lowercase character
 * c is the xter to check
 *
 * Return: 1 if c is lowercase otherwise return 0
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
